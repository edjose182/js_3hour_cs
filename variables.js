/* Data Types:
undefined, null, boolean, string,
symbol, number and object
*/
var myName = 'Trafalgar';
//the variable declared with var
//could be used through the whole
//program

myName = 8;

let ourName = "freCodeCamp";
//Only could be used in the scope where
//the variable was declaired

const pi = 3.14;
//Variable that would never change